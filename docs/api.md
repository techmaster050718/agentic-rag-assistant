# API Documentation

## Chat Endpoints

- `POST /api/v1/chat/stream`: Initiates a streaming conversation with the agent. Uses SSE.

## Ingestion Endpoints

- `POST /api/v1/ingest/upload`: Uploads a document to be chunked and embedded.

## Health Endpoints

- `GET /api/v1/health`: Returns system status and component health checks.
